/** Pointer Events */

/**
 * There are currently two widely used ways to point at things on a screen:
 * mice (including devices that act like mice, such as touchpads and trackballs) and
 * touchscreen. These produce different kinds of events.
 */
